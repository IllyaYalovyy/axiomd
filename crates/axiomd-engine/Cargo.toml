[package]
name = "axiomd-engine"
description = "Markdown engine boundary: source in, typed events with source spans out."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
